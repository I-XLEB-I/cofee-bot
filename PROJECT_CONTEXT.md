# PROJECT_CONTEXT.md

## Проект
Telegram-бот для учёта обслуживания кофейных точек.

Рабочий файл:
`/Users/mark/coffee-bot/bot.py`

### Оперативная сводка точек

Блок `К обслуживанию сегодня` дополнительно показывает по шести действующим
точкам:

- состояние связи;
- число продаж вчера и сегодня;
- время и напиток последней безналичной продажи;
- цвет паузы продаж: до 2 часов / 2–3 часа / 3 часа и больше;
- осторожную подсказку о возможной проблеме с подачей кофе.

Продажи и их время приходят из Telemetron, состояние связи — из Vendista через
защищённый read-only endpoint `telemetron-mcp`. `Бел2` остаётся в истории
обслуживания, но не попадает в оперативную сводку. При сбое внешнего сервиса
обычная информация об обслуживании продолжает работать.

В сводке связь и продажи — независимые сигналы. Справа в таблице: `🟢` —
пауза меньше 2 часов, `🟡` — 2–3 часа, `🔴` — 3 часа и больше, `⚪` — точка
вне графика. Это индикатор активности, а не подтверждение ошибки. Подсказка о
кофе строится по тройной классификации: `с кофе`, `без кофе`,
`не определено`; неизвестный напиток не угадывается. Она выводится одной
короткой строкой только для жёлтой/красной паузы после напитка без кофе.
Фоновый порог мониторинга `no_sales` в 90 минут этим представлением не
изменяется.

Важно:
- backup-файл не трогать
- основной рабочий код сейчас живёт в одном файле `bot.py`
- бот уже развернут в Railway и используется в реальной работе

---

## Инфраструктура

Бот запущен в Railway и работает через polling.

Важно:
- отдельный сервер `185.119.58.78` больше не используется
- актуальный код хранится в GitHub
- Railway подключён к GitHub и автоматически разворачивает свежую версию после push
- ручной деплой через `scp`, `ssh` и `systemctl` больше не используется

Часовой пояс:
- `Europe/Moscow`

Обновление:
```bash
git push origin main
```

После push Railway автоматически запускает новый деплой.

Логи:
- смотреть в интерфейсе Railway в логах активного deployment/service

---

## Основные сущности

### Точки
- Беломорский
- Гагарина
- Гиппо
- Южный
- Сити
- Макси
- Бел2

### Доп. локация для ревизии
- Дома

### Сотрудники
В коде уже есть список сотрудников. Основной рабочий сценарий завязан на Кирилла и Матвея.

---

## Что умеет бот

- Информация по точкам
- Обслуживание точек
- Учёт воды
- Учёт закупок
- Учёт нехватки расходников
- Учёт проезда
- Отчёт за день
- Отчёт за период
- Блок `К обслуживанию сегодня`
- Месячная ревизия
- Импорт ревизии из текста

---

## Бизнес-логика обслуживания

### Режим обслуживания
Точки обслуживаются "через день":
- если точка была обслужена вчера, сегодня её можно не обслуживать
- если была позавчера или раньше, она попадает в список "нужно сегодня"

### Блок `К обслуживанию сегодня`
Есть отдельные блоки:
- `🚨 Срочно пополнить`
- `🔴 Нужно сегодня`
- `🟡 На контроле`
- `⏭ Можно не обслуживать сегодня`
- `✅ Уже обслужены сегодня`

### Критический сигнал
Точка попадает в критический блок, если по расходнику указано:
- `запаса для пополнения нет`
- и `не хватит до следующего приезда`

---

## Даты

### В обслуживании и исправлении записей
Ручной ввод даты:
- формат: `дд.мм`
- пример: `05.04`

Дополнительно:
- зимние даты вроде `30.12` автоматически понимаются как прошлый год
- дату нельзя указывать позже сегодняшней
- дату нельзя указывать старше 1 года

### Важно
Бот работает по московскому времени.

---

## Вода

Вода может быть дробной:
- `2,5`
- `1,2`
- `0,4`

Раньше была проблема, когда где-то показывалось `25` вместо `2,5`.
Это исправлено:
- карточки точки
- экраны редактирования
- чтение из Google Sheets
должны показывать одно и то же значение.

---

## Закупки

Есть стандартные закупки и `Другое`.

Для `Другое` flow должен быть:
1. название
2. количество
3. стоимость

Важно:
- custom-закупка не должна терять стоимость
- `Назад` внутри этого flow должен работать по шагам
- после завершения custom-позиции бот возвращает пользователя в список закупок

---

## Нехватка расходников

Логика нехватки сейчас описывает не весь остаток на точке, а именно:
`сколько осталось в запасе для пополнения`

Для выбранного расходника можно:
- указать остаток в запасе
- нажать `Пропустить`
- нажать `Запаса для пополнения нет`

Если выбран `Запаса для пополнения нет`, бот дополнительно спрашивает:
- `Хватит до следующего приезда`
- `Не хватит до следующего приезда`

Это влияет на критичность точки в блоке `К обслуживанию сегодня`.

### Единицы в нехватке
- Стаканы: туб
- Крышки бел: туб
- Крышки чёрн: туб
- Сахар: стиков
- Манжеты: пачек
- Палочки: пачек
- Трубочки: пачек
- Сиропы: бутылок
- Остальное по текущим единицам в коде

### Отображение
Нехватка должна показываться списком, а не одной длинной строкой.

Пример правильного вида:
- Стаканы — запас: 1 туб
- Сахар — запаса для пополнения нет, не хватит до следующего приезда

---

## Ревизия

Ревизия — это отдельный блок, не смешанный с обычным обслуживанием.

### Период ревизии
Ревизия хранится по месяцам:
- Март 2026
- Апрель 2026
и т.д.

То есть выбирается месяц, а не конкретный день.

### Локации ревизии
- все точки
- Дома

### Порядок товаров в ревизии
Строго в таком порядке:
1. Кофе
2. Молоко
3. Мока
4. Шоколад
5. Стаканы
6. Сахар
7. Сиропы
8. Крышки бел
9. Крышки чёрн
10. Трубочки
11. Палочки
12. Манжеты
13. Вода
14. Влажные салф
15. Мус.пакеты

### На каждом шаге ревизии
Должны быть:
- быстрые кнопки
- `Другое`
- `Пропустить`
- `Назад`

### Что уже есть
- Заполнить ревизию
- Изменить ревизию
- Посмотреть ревизию
- Сравнить с прошлым месяцем

### Изменение ревизии
Нужны режимы:
- изменить один пункт
- пройти всю ревизию заново
- удалить ревизию

### Просмотр ревизии
Есть режимы:
- общая по месяцу
- по точке
- по товару

### Сравнение
Сравнение делается по точке:
- текущий месяц vs прошлый месяц

---

## Google Sheets

### Основной spreadsheet
Используется текущий рабочий spreadsheet, уже подключенный в коде.

### Лист ревизии
Название:
`Ревизия`

Важно:
- бот умеет сам создать лист `Ревизия`
- бот умеет сам добавить заголовки
- одна строка = одна локация за один месяц

---

## Импорт ревизии из текста

Есть отдельная функция:
`📥 Импорт из текста`

Она нужна для старых ревизий, которые сотрудники прислали в чат вручную, когда бот ещё не поддерживал ревизию или не было интернета на точке.

### Какие форматы понимает импорт
1. Блоки по точкам:
```text
02.04 Бел2
Кофе - 3.1
Молоко - 1.4
...
```

2. Табличный формат через `/`:
```text
Сити/юж/бел/б2/гагар/гиппо/дома
Кофе исп - 4/0/5/0/5/5/
Кофе кит - 0/5/0/3/0/0/
...
```

### Особенности импорта
- `Кофе исп` и `Кофе кит` складываются в общее поле `Кофе`
- добавлена поддержка `Салфетки сухие`, потому что они встречаются в исторической ревизии
- перед сохранением бот показывает превью импорта
- бот показывает предупреждения о конфликтующих значениях
- для обычных товаров при конфликте сейчас берётся последнее найденное значение
- для кофе исп/кит значения суммируются

### Практический смысл
Это позволяет не переносить ревизию вручную из старых сообщений.

---

## UX-договорённости

- `Назад` должен возвращать на предыдущий логический шаг
- старые сообщения в Telegram не обновляются автоматически после изменений кода
- после обновления бота экран нужно открывать заново
- длинные технические строки лучше не показывать пользователю, если можно дать компактную карточку
- подтверждения и карточки должны быть короткими и быстро читаемыми

---

## Важно для нового чата

Если работа переносится в новый чат, нужно дать:
1. этот файл/текст
2. путь к рабочему файлу:
   `/Users/mark/coffee-bot/bot.py`
3. фразу:
   `backup-файл не трогать`
4. напоминание:
   `бот работает в Railway; изменения пушить в GitHub, после push Railway обновляется автоматически`

---

## Что обычно проверять после изменений

1. Локально:
```bash
python3 -m py_compile /Users/mark/coffee-bot/bot.py
```

2. Деплой:
```bash
git push origin main
```

После push дождаться успешного автоматического deployment в Railway.

3. Если что-то не работает:
- проверить статус и логи deployment в Railway
- при необходимости откатить deployment через Railway или вернуть рабочий коммит в GitHub
